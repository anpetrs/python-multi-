<details>
  <summary>Степени</summary>

# Степени
оператор возведения в степень (**) является **правоассоциативным** (значение выражения вычисляется справа налево) в соответствии с правилами математики.  
т.е. выражение **x ** y ** z** вычисляется как **x ** (y ** z)**.

`print(2 ** 2 ** 3)     # 2 ** (2 ** 3) = 2 ** 8`
</details>

<details>
  <summary>модуль math</summary>

импортируем так: 

`import math
num = math.sqrt(10)`

или так:

`from math import sqrt
num = sqrt(10)`

# Функции модуля math

| Функция | Описание |
|:---|:---|
| **Округление** | |
| `math.floor(x)` | Округляет число $x$ вниз |
| `math.ceil(x)` | Округляет число $x$ вверх |
| **Корни, степени, логарифмы** | |
| `math.sqrt(x)` | Квадратный корень числа $x$ |
| `math.pow(x, n)` | Возведение числа $x$ в степень $n$ |
| `math.log(x)` | Натуральный логарифм числа $x$ (основание $e$) |
| `math.log10(x)` | Десятичный логарифм числа $x$ (основание 10) |
| `math.log(x, b)` | Логарифм числа $x$ по основанию $b$ |
| `math.factorial(n)` | Факториал натурального числа $n$ |
| **Тригонометрия** | |
| `math.degrees(x)` | Преобразует угол $x$ из радиан в градусы |
| `math.radians(x)` | Преобразует угол $x$ из градусов в радианы |
| `math.cos(x)` | Косинус угла $x$ (в радианах) |
| `math.sin(x)` | Синус угла $x$ (в радианах) |
| `math.tan(x)` | Тангенс угла $x$ (в радианах) |
| `math.acos(x)` | Арккосинус $x$ (в радианах, от $0$ до $\pi$) |
| `math.asin(x)` | Арксинус $x$ (в радианах, от $-\pi/2$ до $\pi/2$) |
| `math.atan(x)` | Арктангенс $x$ (в радианах, от $-\pi/2$ до $\pi/2$) |
| `math.atan2(y, x)` | Полярный угол точки с координатами $(x, y)$ |

# Константы модуля math

| Константа | Значение | Описание |
|:---|:---|:---|
| `math.pi` | 3.141592653589793 | Число $\pi$ (отношение длины окружности к диаметру) |
| `math.e` | 2.718281828459045 | Число $e$ (основание натурального логарифма) |
| `math.tau` | 6.283185307179586 | Число $\tau$ ($2\pi$) |
| `math.inf` | `float('inf')` | Положительная бесконечность |
| `math.nan` | `float('nan')` | Значение "Not a Number" (не число) |

# Функции округления в Python

| Функция | Описание |
|:---|:---|
| `int(x)` | Округляет число в сторону нуля (отбрасывает дробную часть) |
| `round(x)` | Округляет число $x$ до ближайшего целого. Если дробная часть равна 0.5, округляет до ближайшего **четного** целого (банковское округление) |
| `round(x, n)` | Округляет число $x$ до $n$ знаков после десятичной точки |
</details>

<details>
  <summary>Алгоритм получения цифр n-значного числа math</summary>
  
# Алгоритм получения цифр n-значного числа

Последняя цифра: `num % 10`  
Предпоследняя цифра: `(num // 10) % 10`  
Предпредпоследняя цифра: `(num // 100) % 10`  
...  
Третья цифра: `(num // 10 ** (n - 3)) % 10`  
Вторая цифра: `(num // 10 ** (n - 2)) % 10`  
Первая цифра: `num // 10 ** (n - 1)`  

**формула для вычисления i-й цифры n-значного числа num в общем виде**:

`(num // 10 ** (n - i)) % 10`  

### другой алгоритм получения каждой цифры n-значного числа num**:

Последняя цифра: `num % 10`  
Предпоследняя цифра: `(num % 100) // 10`  
Предпредпоследняя цифра: `(num % 1000) // 100`  
...  
Третья цифра: `(num % 10 ** (n - 2)) // (10 ** (n - 3))`  
Вторая цифра: `(num % 10 ** (n - 1)) // (10 ** (n - 2))`  
Первая цифра: `(num % 10 ** n) // (10 ** (n - 1))`  

**формула для вычисления i-й цифры n-значного числа num в общем виде**:

`num % 10 ** (n + 1 - i) // (10 ** (n - i))`

</details>

<details>
  <summary>формулы</summary>

### Формула среднего арифметического (середины диапазона)

$$\text{Average} = \frac{\text{min} + \text{max}}{2}$$

### Максимин (Maximin)

`result = max(min_a, min_b)`

</details>
